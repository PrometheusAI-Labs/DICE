version: '3.8'

services:
  bot:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: telegram-dice-bot
    ports:
      - "${PORT:-5001}:5000"
    environment:
      - BOT_TOKEN=${BOT_TOKEN}
      - PORT=5000
      - RUST_LOG=info
    restart: unless-stopped
